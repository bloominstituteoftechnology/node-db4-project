const db = require('./../../../data/db-config');

function getAllIngredients(recipe_id){}

function addIngredientToStep(newIngredient){}

function updateIngredientAmt(updatedAmt){}

function updateIngredient(updatedIngredient){}

function deleteIngredientFromRecipe(deletedIngredient){}

module.exports = {
    getAllIngredients,
    addIngredientToStep,
    updateIngredientAmt,
    updateIngredient,
    deleteIngredientFromRecipe
}